# Never commit fetched pages, crawl state or generated datasets.
.raw/
.state/
dist/
*.sqlite
*.sqlite-shm
*.sqlite-wal
# Local-only overrides (delays/caps can only be made more conservative).
medicine-data.config.json
node_modules/
