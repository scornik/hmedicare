{
  "name": "@hmedic/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Logger, redaction, correlation, metrics",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "pino": "10.3.1",
    "prom-client": "15.1.3"
  }
}
